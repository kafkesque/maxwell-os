# SYNC 2026-09-17 — governance dependencies updated with the BUG-262..266 revelations

Verification command (run it, do not trust this prose):
    python3 scripts/drift_monitor.py
    python3 tools/bench_preflight.py --selftest
    python3 scripts/validate_role_claims.py

## 1. What changed in the record

| id | what it was | magnitude | status |
|---|---|---|---|
| BUG-262 | benchmark harness omitted the reasoning-off prefix for gpt-oss | gpt-oss 0.00 on toolcalling, 20/20 no-json | FIXED |
| BUG-263 | S2 relabel judge hardcoded max_tokens=64 vs a Harmony reasoning model | LATENT. Generator judge (gemma) unaffected; the truly silent path was the cross-family FLAG layer, whose failures were never in the exit condition | FIXED + fail-closed guard |
| BUG-264 | _done_keys() skips by key, so a row measured by a broken harness is never re-measured | 40 poisoned rows (20 gpt-oss, 20 gemma) | purged, archived, guarded |
| BUG-265 | call() read content only, never tool_calls | gemma 0.55 to 0.60 (+1 item) — early claim of a total failure was OVERSTATED and corrected | FIXED |
| BUG-266 | gpt-oss toolcalling truncated (finish_reason=length) | NA cell, not a 0.00 | declared NA |
| gate defects | 3 found by USING the gate: JSON predicate wrong for prose-graded suites, --suites argparse, concurrency guard matched its own launcher | 9 cells falsely NA, 1 run blocked | all FIXED |

## 2. The R5 audit (new) — which verifications were never cross-family

R5/C8 requires a different FAMILY, not a different model. The corpus generator is Qwen3-Coder.

| script | judge | family vs generator | verdict |
|---|---|---|---|
| dedup_judge.py | Qwen3.8-27B | Qwen vs Qwen | NOT cross-family |
| judge_suffix_duplicates.py | Qwen3.8-27B | Qwen vs Qwen | NOT cross-family |
| reverify_content_type_108.py | Qwen3.8-27B | Qwen vs Qwen | NOT cross-family |
| classify_promotion_labels.py | Qwen3.8-27B | Qwen vs Qwen | NOT cross-family |
| apply_alias_corrections.py | Qwen3.8-27B | Qwen vs Qwen | NOT cross-family |
| deepseek_frontier_audit.py | Qwen3.8-27B (VERIFIER_MODEL) | Qwen vs Qwen | NOT cross-family |
| disagreement_sample.py (Aug 23) | gemma-4-E4B | DIFFERENT | valid: 200-row sample, empty 0, human adjudication gemma overruled Qwen 36/49 |
| relabel sweep (FORM, full corpus) | gemma-4-E4B | DIFFERENT | valid on 5761 single-source candidates |

Scope to re-verify is the DECISION LISTS, not the corpus: 299 dedup pairs + 209 merge groups + 108
content-type + 69 frontier + 76 aliases + 8 promotions = roughly 770 judge calls.

## 3. The FORM axis: measured cross-family agreement

From governance/relabel_report_20260917_123804.json and the per-record cross audit (n=200):
- gemma re-judging its own historical labels: 194/200 agree (97 percent) - the stored labels are reproducible.
- gpt-oss (independent family) matches the stored label 106/200 (53 percent) vs 25 percent chance.
- gemma proposed a change on only 6/200; cross-family disagreements flagged 5; cross-family failures 0.

Interpretation: the FORM axis is reproducible WITHIN a family and only weakly ACROSS families. That is
a declared limitation, not a bug: label FORM as family-dependent, and do not present it as an objective
property of the record. It also means the Aug-23 gemma-overrules-Qwen result should be read as one
family's preference, not as ground truth.

## 4. RERUN MATRIX — what must be re-run when a component changes

| change | must re-run | need NOT re-run | cost |
|---|---|---|---|
| S2 DSPy prompt consolidation | S2 extract on all clusters, then S4 merge/classify, S5 verify, S6 commit; the FORM relabel (decision order lives in the S2 prompt); the 2641 convergent records that were never relabeled | S0 convert, S1 chunk, S1.5 embed (text unchanged) | FULL CHAIN - the expensive one |
| S4 finetuned student (ModernBERT) | S4 classification only, then S4_5 enrich, S6 commit, domain aggregation, taxonomy promotion | S0-S2, S1.5, S5 NLI, embeddings | MEDIUM - label-only rerun |
| S4 classifier model swap | same as above + the S4 gold evaluation | S2, S1.5 | MEDIUM |
| R5 re-verification of the 6 axes | the 770-call decision lists above | the corpus text and labels | ~30-45 min |
| embedding model change | S1.5 clustering, all FAISS indexes, retrieval eval, hyDE, contextual embed | S2-S6 labels (unless clusters move) | MEDIUM-HIGH |
| NLI model change | S5 verify, S6 commit | S0-S4 | LOW |
| model portfolio / role change | the benchmark suites only | the corpus | LOW (no oMLX writes) |

Rule of thumb: an S2 change cascades to everything downstream; an S4 change touches only labels and
their consumers. Nothing above requires re-running S0, S1 or S1.5 except an embedding change.

## 5. Which stages need finetune, which need a fix, which are fine

| stage | status | action |
|---|---|---|
| S0 convert / S1 chunk / S1.5 embed | fine | none - no LLM, deterministic |
| S2 extract | needs consolidation | DSPy program exists but dspy_enabled false; role S2_DSPY_TARGET is UNVERIFIED and uncited. Single-source FORM drifted before the gemma relabel; convergent 2641 never relabeled |
| S2 relabel | fixed today | FLAG layer now runs for the first time (200/200 judged, 0 failures) |
| S4 merge/classify | fine, but depth is unsolved | discipline/domain measured on the production path; DEPTH is a prompt+gold problem, not a model choice - no model predicts 'none' (0/60) and 5 gold rows are 'none', so every model caps at 0.917 |
| S4 student | EXPERIMENT | classifier_training -> ModernBERT-base, checkpoint classifier_modernbert_p5final. Gate it on the same gold rows before it replaces gpt-oss |
| S0.5 metadata | fix owed | probe shows valid 1.00 but pair_hit 0.58, title_side 0.33 - the grader may be the weak part |
| S4_5 enrich | fix owed | 0/12 procedural_skill ever written; snake_case 0.00 |
| S5 verify | fine | DeBERTa NLI, fail-closed |
| S6 commit | fine | SQLite + Parquet |

## 6. Open gates

- P1: tool-calling and planning roles now carry n=20 / n=24 evidence. DONE.
- P2: freeze the three config files. Gate = validate_role_claims.py at 0 uncited, or every remainder stamped UNVERIFIED.
- P3: gpt-oss toolcalling re-measure and the BUG-257 niah ceiling close-out.
- Carried: the 6 R5 re-verifications; the 2641 convergent FORM records; reporting deepseek-v4-pro as the offline anchor-builder only.

