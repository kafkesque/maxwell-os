# Maxwell OS v3.0 — Minimal dependencies
# Authority: CONSTITUTION.md C5 (Zero bloat)
# Updated: 2026-08-05 — D2157: added faiss, sentence-transformers, transformers

# ── Pipeline core ────────────────────────────────────
pydantic>=2.0
pyyaml>=6.0
tqdm>=4.0
psutil>=6.0            # D2341/BUG-098: memory_guard.py, run_monitor.py, run_diagnostic.py, n2_watchdog.py (C24 hardware-adaptive)

# ── Data storage ─────────────────────────────────────
# sqlite3 is stdlib (no pip needed)
pyarrow>=14.0          # Parquet export

# ── LLM inference (local, C1) ───────────────────────
requests>=2.28         # OMLX/Ollama HTTP calls
# ollama>=0.4           # D2202: INTENTIONALLY NOT INSTALLED. All Ollama calls use requests HTTP API directly.
#                       # The ollama Python package would add a daemon-management dependency.
#                       # If added in future, also update ollama_embed.py which currently delegates
#                       # single-doc path to batch_embed (requests-based).

# ── Embeddings & clustering ─────────────────────────
numpy>=1.24
scikit-learn>=1.3      # Cosine similarity utilities
# D2177: umap-learn + hdbscan removed (Stage 3 dead, D2120). C5 + C19 compliance.
faiss-cpu>=1.7         # FAISS cosine nearest-neighbor search (S1.5)
sentence-transformers>=2.2  # MPS embedding via bge-small (S1.5, coverage_check)

# ── NLI verification (S5) ────────────────────────────
transformers>=4.40     # ModernBERT/DeBERTa NLI pipeline

# ── Clustering ────────────────────────────────────────
networkx>=3.2          # D2177: Louvain community detection (S1.5, C11)

# ── Deduplication ────────────────────────────────────
datasketch>=1.6        # MinHash near-dedup

# ── Vector search ────────────────────────────────────
sqlite-vec>=0.1        # P0.11: Vector search in SQLite

# ── DSPy optimization (post-T1.1, GAP-1) ────────────
dspy-ai>=3.3.0         # D2243/D2304: MIPROv2 optimizer + DirectOMLXLM (dspy_trainer.py)
